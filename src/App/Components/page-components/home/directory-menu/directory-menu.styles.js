// JSS HOOK
import { createUseStyles } from 'react-jss';

export const directoryMenuStyles = createUseStyles({
  directoryMenu: {
    width: '100%',
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'space-between'
  }
});